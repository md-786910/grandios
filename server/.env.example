# Server Configuration
PORT=8000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/grandios

# JWT
JWT_SECRET=grandios-secret-key-change-in-production
JWT_EXPIRE=7d

# External API (for later integration)
EXTERNAL_API_URL=https://apidocs.wawi.onretail.eu/rest
EXTERNAL_API_DB=a5d5fec838

# SMTP (for password reset - optional for now)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
